<!doctype html>
<html lang="en-US">
  <head>
  </head>
  <body>
    <% if session[:message] %>
      <p><%= session.delete(:message) %></p>
    <% end %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
