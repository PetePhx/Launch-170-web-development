<!doctype html>
<html lang="en-US">
  <head>
    <title><%= @title %></title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/stylesheets/cms.css">
  </head>
  <body>
    <% if session[:message] %>
      <div class="message"><%= session.delete(:message) %></div>
    <% end %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
